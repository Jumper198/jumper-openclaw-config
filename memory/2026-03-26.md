
## 2026-03-26: 战略升级与技术安全审计 (The OPC OS & OpenCLI Audit)

### 1. 核心战略定位确认：从“IP 流程中心”到“OPC OS 网关”
- Jumper 已成功推进与上海徐汇原力社区（Force Space）副书记及凌总的会谈。
- **现状确认**：政府算力券细则尚未落地，但语料券（Kupasai 供应商）已确定由原力社区统管。
- **战略降维打击**：放弃传统 SaaS 卖点，全力主攻**“区级算力与语料全域核销与防泄密网关（OPC OS）”**。通过提供防刷单审计、零信任联邦沙盒（可用不可见），帮助政府解决发券后的资产流失与合规安全痛点，换取 300 万示范项目补贴及全园区算力路由垄断权。

### 2. 商业博弈与合伙人排雷
- **识别无效资源**：合伙人 D 在关键的跨园区（如北杨小镇）和向上对接（处级大模型公司总经理）上表现出严重的人脉匮乏（“好的我问问”盲盒话术）。
- **执行策略调整**：Jumper 必须从对合伙人的依赖中独立，转为“独狼过江”模式。利用已有的高管微信（原力空间副书记主动添加），通过高保真、直击监管痛点的前端大屏 Demo 视频（Inbound Marketing）进行“越级钓鱼”，直接锁定具备预算审批权的处级领导。
- **对冲“无预付”国企规则**：面对国企项目 0 首付的现实，采用“供应链金融（合同贷）”、“要求官方全额提供开发硬件（OpEx 清零）”、“里程碑切片验收”以及“针对 2500 个 OPC 收取云端沙盒租赁费（B2B 变现）”的四重杠杆对冲资金链断裂风险。

### 3. 底层技术安全阻断：OpenCLI (opencli-rs) 审计
- **工具分析**：Jumper 拟引入 `opencli-rs`（Rust 重写的浏览器复用与 CLI 穿透神兵）作为大模型抓取语料的底层 Skill。
- **安全红线警报**：该工具通过 CDP/扩展接管全量浏览器 Session，且支持 YAML 注入任意 JS（`evaluate`）和主机高危命令透传（`kubectl`, `docker`）。若部署于生产网关或日常宿主机，极易导致 Auth Token 被盗和 RCE 提权。
- **强制隔离令**：**严禁在开发主力和生产网关上安装此 Skill**。必须将其物理隔离至专用的“语料清洗肉鸡/沙盒虚拟机”中，使用专属小号，并禁用 `external-clis.yaml` 中的越权指令，仅作为无头数据矿工使用。

### 4. 基础设施确认与语料矿机部署 (VM Infrastructure & opencli-rs Deployment)
- **环境确认**：Jumper 确认当前 OpenClaw 运行在物理隔离的虚拟机（VM）中。这彻底解除了 `opencli-rs` 工具带来的宿主机 RCE 和核心资产提权风险。
- **工具部署**：已成功在 VM 环境中审计并安装了 `opencli-rs` (v0.1.0) 至 `~/.local/bin/`。诊断显示环境安全（无高危 kubectl/docker 暴露）。
- **OPSEC 纪律**：确立了该 VM 作为“语料自动化挖掘与清洗智能体（数据矿机）”的定位。严禁在 VM 内登录 Jumper 的个人真实账号（微信、网银、主力 GitHub），强制要求使用“爬虫马甲号”。
- **后续动作（Pending）**：需 Jumper 手动在 VM 中启动 `opencli-rs daemon` 并安装 Chrome 扩展，以彻底打通基于浏览器 Session 的全网数据抓取链路。

### 5. Xiaohongshu (小红书) 自动化获客矩阵实战与失败复盘
- **业务动作**：使用 `xiaohongshu-mcp` 成功发布了多条获客引流笔记，试图通过“AI 替代低效人力”、“OPC OS 网关”等痛点文案吸引 ToB 客户。
- **技术盲区与踩坑 (Text-to-Image)**：
  - 小红书原生 APP 支持高转化率的“文字转长图（备忘录风）”，但网页版（PC Web Creator Center）接口无此功能。
  - **初次尝试**：徒手写 Python `Pillow` 脚本调用系统 `PingFang.ttc` 字体渲染 1080x1440 长图并发布。
  - **结果**：图片在小红书 CDN 渲染失败（“图挂了”）。
- **工具链遗忘与自我纠偏**：Jumper 指出系统已配置了 `nanobanba` (Nano Banana Pro / Gemini 3 Pro Image) 的 API 密钥，但我遗忘了其存在（一直在找旧路径 `generate_image.py`），导致徒手造轮子失败。
- **Next Step**：必须立刻改用正确的 `image_generate` 工具（配置为 `nano-banana-pro`）或对应的 Skill 重新生成高转化率图文，停止使用低效的本地 Python 脚本渲染。
