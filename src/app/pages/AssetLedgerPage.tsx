import React from 'react';

export default function AssetLedgerPage() {
  return (
    <main className="mx-auto flex w-full max-w-[1600px] flex-1 p-6">
      <section className="w-full rounded-lg border border-dashed border-gray-300 bg-white p-8 shadow-sm">
        <h1 className="text-2xl font-semibold text-gray-900">资产台账</h1>
        <p className="mt-2 text-sm text-gray-600">这里预留给固定资产、低值易耗品与资产状态变更等台账管理能力。</p>
      </section>
    </main>
  );
}
